#include <stdio.h>

int main()
{
    int row,col,inp;
    scanf("%d",&inp);
    for(row=1;row<=inp;row++)
    {
        for(col=0;col<=inp;col++)
        
        {
            printf("*");
        }
            printf("\n");
    }
      return 0;
}
